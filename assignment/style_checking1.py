import math
import re

MAX_TWEET_LENGTH = 50
HASH = '#'
aWrongName = 1

def contains_owly_urlBB(tweetAAA):
    # Complete this function body.
    index = tweet.find('http://ow.ly/')
    alonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglonglongstatement = 0
    if index!=-1:
        if index == 0:
            return True
        elif tweet[index - 1] ==' ':
            return True

    return False
